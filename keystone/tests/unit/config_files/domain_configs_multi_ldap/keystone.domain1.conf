# The account-specific configuration file for the test account
# 'account1' for use with unit tests.

[ldap]
url = fake://memory1
user = cn=Admin
password = password
suffix = cn=example,cn=com

[identity]
driver = keystone.identity.backends.ldap.Identity